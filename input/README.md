# Input folder

After running the Colab notebook, upload the generated `source_video.zip` here.

Expected path for GitHub Actions:

`input/source_video.zip`

The workflow also accepts `input/*.mp4` directly if you prefer to upload the compressed MP4 rather than a zip.
